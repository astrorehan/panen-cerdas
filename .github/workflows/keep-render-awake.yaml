name: Keep Render Awake

on:
  schedule:
    # Jalan tiap 5 menit
    - cron: "*/5 * * * *"

  # Bisa dijalankan manual dari GitHub Actions tab
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render App
        run: |
          curl -I https://panen-cerdas-ml.onrender.com