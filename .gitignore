# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Node / Next.js (frontend has its own .gitignore too)
node_modules/
.next/
out/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / credentials
.env
.env.local
.env*.local
*.pem
service-account*.json
ee-credentials.json

# ML service runtime artifacts
ml_service/.env
# saved_models/: trained joblib artifacts ARE tracked (committed from PanenPintar-V2).
# Regenerate with `python ml_service/train.py` if you need to retrain.
ml_service/panencerdas_ml.db
ml_service/*.db
panencerdas_ml.db
*.db

# Data (raw downloads are heavy — share via Drive)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/models/*
!data/models/.gitkeep
data/shapefiles/*
!data/shapefiles/.gitkeep

# Runtime ML logs (predictions + feedback per developer)
data/predictions.jsonl
data/feedback.jsonl

# NASA POWER on-disk cache (regenerated on demand)
data/cache/

# IDE
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Vercel
.vercel

# Claude Code local state (not shared across team)
.claude/
SESSION_HANDOFF.md

# OS
desktop.ini

# Build
dist/
build/
*.log
